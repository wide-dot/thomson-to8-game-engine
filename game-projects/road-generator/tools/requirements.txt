# Outils data piste road-generator (road_bake.py, road_preview.py, render_road_frame.py).
#
# tkinter n'est PAS un paquet pip — il vient du Python de base. Le venv DOIT être
# créé depuis un Python qui a tkinter avec Tk 8.6+ (Tk 8.5 d'Apple = fenêtre VIDE
# sur macOS récent).
#
#   macOS  : brew install python-tk@3.12
#            /opt/homebrew/bin/python3.12 -m venv .venv        (Tk 9.0 — OK)
#            ⚠️ NE PAS utiliser /usr/bin/python3 (Tk 8.5 = cassé).
#   Linux  : sudo apt install python3-tk ; python3 -m venv .venv
#   Windows: python -m venv .venv                              (Tk inclus)
#
#   puis : .venv/bin/pip install -r tools/requirements.txt
#   lancer le preview : .venv/bin/python3 tools/road_preview.py [circuit]
Pillow>=9.0
