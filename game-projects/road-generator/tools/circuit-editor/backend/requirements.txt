# Backend éditeur de circuits — s'installe DANS le venv existant (.venv, python3.12)
#   .venv/bin/pip install -r tools/circuit-editor/backend/requirements.txt
# (le pipeline lui-même = road_bake/simulate_dfr_pipeline dans tools/ ; Pillow requis pour l'export/recolor)
fastapi
uvicorn[standard]
httpx
Pillow
